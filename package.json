{
  "name": "contract-guardian",
  "version": "1.0.0",
  "description": "Full-stack application for analyzing contract documents",
  "scripts": {
    "start": "node Backend/api/server.js",
    "build": "cd Frontend && npm install && npm run build",
    "postinstall": "npm run build",
    "dev": "nodemon Backend/api/server.js"
  },
  "engines": {
    "node": "16.x",
    "npm": "8.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cookie-parser": "^1.4.6",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.11.3",
    "python-shell": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
